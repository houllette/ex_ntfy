import Config

# Production environment configuration

config :logger,
  level: :info

# Example: Configure production-specific settings
# Most production configuration should be in runtime.exs
# Only compile-time configuration should be here
